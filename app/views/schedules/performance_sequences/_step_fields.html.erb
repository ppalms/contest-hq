<tr
  data-sequence-target="step"
  data-new-record="<%= f.object.persisted? ? 'false' : 'true' %>"
>
  <td class="hidden">
    <%= f.hidden_field :id if f.object.persisted? %>
    <%= f.hidden_field :_destroy, data: { sequence_target: "destroy" } %>
  </td>
  <td class="w-12">
    <%= f.text_field :ordinal, class: "text-field" %>
  </td>
  <td>
    <%= f.text_field :name, class: "text-field", data: { sequence_target: "stepName" } %>
  </td>
  <td class="w-72">
    <%= f.select :room_id,
             rooms.order(:name).map { |t| [t.name, t.id] },
             { include_blank: "Select a room" },
             { required: true, class: "text-field" } %>
  </td>
  <td class="w-20">
    <%= f.text_field :duration, class: "text-field text-right", required: true %>
  </td>
  <td>
    <%= button_tag type: "button",
        class: "btn-danger-sm",
        data: { action: "sequence#removeStep" } do %>
      Delete
    <% end %>
  </td>
</tr>
