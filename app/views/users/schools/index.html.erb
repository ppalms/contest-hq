<%= render "shared/searchable_selection",
      title: "Add Schools for #{@user.first_name} #{@user.last_name}",
      search_url: user_schools_path(@user, from_invitation: params[:from_invitation]),
      form_target: @user,
      form_url: user_schools_path(@user, from_invitation: params[:from_invitation]),
      items: @schools,
      pagy: @pagy,
      search_param: :search,
      search_placeholder: "Search schools...",
      item_name_method: :name,
      item_id_method: :id,
      selected_items: @user.schools,
      selection_field_name: "user[school_ids][]",
      cancel_url: params[:from_invitation] ? new_invitation_path : edit_user_path(@user) %>